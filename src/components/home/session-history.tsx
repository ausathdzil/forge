import { Subheading } from '../typography'

/**
 * @todo: list of sessions
 * - show today's session last week, then the rest of the sessions
 */
export function SessionHistory() {
  return (
    <div className="text-center">
      <Subheading>History</Subheading>
    </div>
  )
}
